<?xml version="1.0" encoding="utf-8"?>
<openerp>
  <data noupdate="0">
        <record id="email_template_rental_reminder" model="email.template">
            <field name="name">Enviar Recordatorio de Alquiler</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Recordatorio de Alquiler para ${object.partner_id.name}</field>
            <field name="email_to">${object.partner_id.email and object.partner_id.email or ''}</field>
            <field name="model_id" ref="sale_rental.model_sale_rental"/>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.lang}</field>
            <field name="body_html"><![CDATA[
                """ 
                Write here a body of email using HTML tag.....
                """            
            ]]></field>
        </record>
        
        <record id="email_template_rental_delay" model="email.template">
            <field name="name">Enviar Recordatorio de Retraso en Alquiler</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Retraso de Alquiler para ${object.partner_id.name}</field>
            <field name="email_to">${object.partner_id.email and object.partner_id.email or ''}</field>
            <field name="model_id" ref="sale_rental.model_sale_rental"/>
            <field name="auto_delete" eval="True"/>
            <field name="lang">${object.lang}</field>
            <field name="body_html"><![CDATA[
                """ 
                Write here a body of email using HTML tag.....
                """            
            ]]></field>
        </record>
  </data>
</openerp>
